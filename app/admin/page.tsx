"use client";
import { useState } from "react"; import { useRouter } from "next/navigation";
export default function AdminLogin(){const [error,setError]=useState("");const router=useRouter();async function submit(data:FormData){const r=await fetch("/api/admin/session",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({passphrase:data.get("passphrase")})});if(r.ok){router.push("/admin/leads");router.refresh()}else setError("That passphrase was not accepted.")}return <main className="grid min-h-screen place-items-center p-6"><form action={submit} className="w-full max-w-md border border-[#b9dfe4] bg-[#ffffff] p-8"><p className="text-xs uppercase tracking-widest text-[#007789]">Private workspace</p><h1 className="mt-2 text-4xl">Lead desk</h1><label className="mt-8 block text-sm">Admin passphrase<input required type="password" name="passphrase" className="mt-2 w-full border border-[#9bcbd2] bg-white px-4 py-3" /></label>{error&&<p className="mt-3 text-sm text-red-700">{error}</p>}<button className="mt-5 w-full bg-[#004f5b] px-5 py-3 text-white">Continue</button></form></main>}
